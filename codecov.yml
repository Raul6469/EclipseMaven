ignore:
  - "src/main/java/org/apache/maven/App.java"

coverage:
  range: 30..75

  status:
      project:
        default:
          if_no_uploads: error
          if_ci_failed: error
      patch:
        default:
          if_no_uploads: error
          if_ci_failed: error